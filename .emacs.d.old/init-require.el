(let
    (( requirements
       '(
         color-theme
         color-theme-scanner-brightly
         recentf
         uniquify
         el-get
         flymake
         ido
         saveplace
         tramp
         )))
  (mapc 'require requirements))
