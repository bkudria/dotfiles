# Fix prompt template YAML files
fix_prompt() {
  [ -z "$1" ] && echo "Usage: fix_prompt <template_name>" && return 1
  local file="$(llm templates path)/${1}.yaml"
  [ ! -f "$file" ] && echo "Error: $file not found" && return 1
  fix_yaml "$file"
}